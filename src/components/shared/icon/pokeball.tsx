import React from 'react';

import {GenericIconBase} from '@/components/shared/icon/base';
import {IconProps} from '@/components/shared/icon/type';


export const GenericPokeballIcon = (props: IconProps) => {
  return <GenericIconBase src="/images/generic/pokeball.png" {...props}/>;
};
