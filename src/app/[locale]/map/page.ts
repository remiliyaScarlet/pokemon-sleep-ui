import {MapIndex} from '@/ui/map/index/main';
import {generatePageMeta} from '@/utils/meta';


export const generateMetadata = generatePageMeta({namespace: 'UI.Metadata.Map.Index'});

export default MapIndex;
