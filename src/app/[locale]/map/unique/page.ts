import {MapUniqueIndex} from '@/ui/sleepStyle/mapUnique';
import {generatePageMeta} from '@/utils/meta';


export const generateMetadata = generatePageMeta({key: 'SleepStyle.UniqueMap.Index.Title'});

export default MapUniqueIndex;
