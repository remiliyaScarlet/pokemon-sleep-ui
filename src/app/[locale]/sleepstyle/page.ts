import {SleepStyleIndex} from '@/ui/sleepStyle/index/main';
import {generatePageMeta} from '@/utils/meta';


export const generateMetadata = generatePageMeta({key: 'SleepStyle.Index.Title'});

export default SleepStyleIndex;
