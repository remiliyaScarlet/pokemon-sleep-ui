import {BerryIndex} from '@/ui/berry/index/main';
import {generatePageMeta} from '@/utils/meta';


export const generateMetadata = generatePageMeta({key: 'Berry.Index.Title'});

export default BerryIndex;
