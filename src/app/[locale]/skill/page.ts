import {SkillIndex} from '@/ui/skill/index/main';
import {generatePageMeta} from '@/utils/meta';


export const generateMetadata = generatePageMeta({key: 'Meal.Index.Title'});

export default SkillIndex;
