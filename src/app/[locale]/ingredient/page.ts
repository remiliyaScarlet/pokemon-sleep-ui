import {IngredientIndex} from '@/ui/ingredient/index/main';
import {generatePageMeta} from '@/utils/meta';


export const generateMetadata = generatePageMeta({key: 'Ingredient.Index.Title'});

export default IngredientIndex;
