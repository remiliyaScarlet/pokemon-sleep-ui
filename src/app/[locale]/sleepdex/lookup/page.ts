import {SleepdexLookup} from '@/ui/sleepStyle/sleepdex/lookup/main';
import {generatePageMeta} from '@/utils/meta';


export const generateMetadata = generatePageMeta({key: 'SleepStyle.Sleepdex.Lookup.Title'});

export default SleepdexLookup;
