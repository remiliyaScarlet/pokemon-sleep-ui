import {ItemIndex} from '@/ui/item/index/main';
import {generatePageMeta} from '@/utils/meta';


export const generateMetadata = generatePageMeta({key: 'Item.Index.Title'});

export default ItemIndex;
