import {SubSkillInfo} from '@/ui/info/subskill/main';
import {generatePageMeta} from '@/utils/meta';


export const generateMetadata = generatePageMeta({key: 'Info.SubSkill.Title'});

export default SubSkillInfo;
