import {InfoIndex} from '@/ui/info/index/main';
import {generatePageMeta} from '@/utils/meta';


export const generateMetadata = generatePageMeta({key: 'Info.Index.Title'});

export default InfoIndex;
