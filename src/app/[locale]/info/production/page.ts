import {ProducingParams} from '@/ui/info/production/main';
import {generatePageMeta} from '@/utils/meta';


export const generateMetadata = generatePageMeta({key: 'Info.ProducingParams.Title'});

export default ProducingParams;
