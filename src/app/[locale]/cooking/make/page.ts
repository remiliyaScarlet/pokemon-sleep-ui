import {MealMaker} from '@/ui/cooking/make/main';
import {generatePageMeta} from '@/utils/meta';


export const generateMetadata = generatePageMeta({key: 'Cooking.Make.Title'});

export default MealMaker;
