import {TeamIndex} from '@/ui/team/index/main';
import {generatePageMeta} from '@/utils/meta';


export const generateMetadata = generatePageMeta({key: 'Team.Index.Title'});

export default TeamIndex;
