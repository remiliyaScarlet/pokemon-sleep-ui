import {PokemonExpCalculator} from '@/ui/xp/main';
import {generatePageMeta} from '@/utils/meta';


export const generateMetadata = generatePageMeta({key: 'PokemonExp.Title'});

export default PokemonExpCalculator;
